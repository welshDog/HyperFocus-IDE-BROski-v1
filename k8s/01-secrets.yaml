apiVersion: v1
kind: Secret
metadata:
  name: hypercode-secrets
  namespace: hypercode
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values with actual secrets
  # For production, use external secret management (e.g., sealed-secrets, external-secrets-operator, vault)
  anthropic-api-key: "REPLACE_WITH_ACTUAL_KEY"
  hypercode-jwt-secret: "REPLACE_WITH_ACTUAL_SECRET"
  postgres-password: "REPLACE_WITH_ACTUAL_PASSWORD"
  redis-password: "REPLACE_WITH_ACTUAL_PASSWORD"
  grafana-admin-password: "REPLACE_WITH_ACTUAL_PASSWORD"
---
apiVersion: v1
kind: Secret
metadata:
  name: api-keys
  namespace: hypercode
type: Opaque
stringData:
  api-key: "REPLACE_WITH_ACTUAL_API_KEY"
  memory-key: "REPLACE_WITH_ACTUAL_MEMORY_KEY"
